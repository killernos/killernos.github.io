import assert from 'node:assert/strict';
import { adaptCommunityReport, ingestCommunityReports } from '../runtime/next-1302/community-report-adapter.js';
const report={submissionId:'NEXT-SUBMIT-A',report:{firmware:'13.02',sessionId:'NEXT-SESSION-ONE',firmwareSource:'user-agent',simulated:false,hardwareDetected:true,imported:false,queryDerived:false,userEntered:false,storageDerived:false,kernelExecutionObserved:false,kernelWriteObserved:false,henObserved:false,diagnostics:[{stage:'NEXT-1302-HARDWARE-CONFIRMED',sessionId:'NEXT-SESSION-ONE'},{stage:'SLOPKIT-CARRIER-OBTAINED',sessionId:'NEXT-SESSION-ONE'},{stage:'SLOPKIT-WINDOW-P-INSTALLED',sessionId:'NEXT-SESSION-ONE'},{stage:'SLOPKIT-READ-VERIFIED',sessionId:'NEXT-SESSION-ONE'},{stage:'SLOPKIT-WRITE-VERIFIED',sessionId:'NEXT-SESSION-ONE'},{stage:'USERLAND-ARW-VERIFIED',sessionId:'NEXT-SESSION-ONE'},{stage:'NEXT-1302-SESSION-COMPLETED',sessionId:'NEXT-SESSION-ONE'}]}};
const adapted=adaptCommunityReport(report);assert.equal(adapted.ok,true);assert.equal(adapted.observation.firmware,'13.02');assert.equal(adapted.observation.samples.every(x=>x.value===true),true);
const duplicate=structuredClone(report);duplicate.submissionId='NEXT-SUBMIT-B';
const batch=ingestCommunityReports([report,duplicate]);assert.equal(batch.accepted,1);assert.equal(batch.duplicateCount,1);assert.equal(batch.observations[0].sessionId,'NEXT-SESSION-ONE');
for(const bad of [{...report,report:{...report.report,simulated:true}},{...report,report:{...report.report,hardwareDetected:false}},{...report,report:{...report.report,firmwareSource:'query'}},{...report,report:{...report.report,firmware:'12.00'}}])assert.equal(adaptCommunityReport(bad).ok,false);
assert.equal(adaptCommunityReport({...report,report:{...report.report,diagnostics:report.report.diagnostics.map(entry=>({stage:entry.stage}))}}).ok,false);
console.log('community report adapter regression checks passed');
